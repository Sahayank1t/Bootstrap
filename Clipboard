You can achieve this by converting the array data into an HTML table and then copying it to the clipboard. Here's a basic example of how you can do this using JavaScript:

```html
<!DOCTYPE html>
<html>
<head>
    <title>Copy Array Data to Clipboard</title>
</head>
<body>

<button onclick="copyToClipboard()">Copy Array Data</button>

<script>
function copyToClipboard() {
    var dataArray = [
        ["Name", "Age", "Gender"],
        ["John", 30, "Male"],
        ["Jane", 25, "Female"],
        ["Doe", 40, "Male"]
    ];

    // Convert array data to HTML table
    var table = "<table><thead><tr>";
    for (var i = 0; i < dataArray[0].length; i++) {
        table += "<th>" + dataArray[0][i] + "</th>";
    }
    table += "</tr></thead><tbody>";
    for (var j = 1; j < dataArray.length; j++) {
        table += "<tr>";
        for (var k = 0; k < dataArray[j].length; k++) {
            table += "<td>" + dataArray[j][k] + "</td>";
        }
        table += "</tr>";
    }
    table += "</tbody></table>";

    // Create a temporary textarea element to copy the HTML table
    var tempTextArea = document.createElement("textarea");
    tempTextArea.value = table;
    document.body.appendChild(tempTextArea);

    // Select the text in the textarea and copy it to the clipboard
    tempTextArea.select();
    document.execCommand("copy");

    // Remove the temporary textarea
    document.body.removeChild(tempTextArea);

    alert("Array data copied to clipboard!");
}
</script>

</body>
</html>
```

This code creates a simple HTML table from the array data and copies it to the clipboard when the button is clicked. You can then paste this table into an email body, and it will be represented as a table.
